import { PageHead } from '@/components/app/app-shell';

export default function AssetDetailStub() {
  return (
    <>
      <PageHead title="Asset detail" sub="Hero product surface — verdict panel, authority graph, records, lifecycle. Phase 3." />
      <div className="v-card v-mono">usd-yield-001.acme.verdict.eth</div>
    </>
  );
}
