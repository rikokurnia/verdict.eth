import { PageHead } from '@/components/app/app-shell';

export default function Page() {
  return (
    <>
      <PageHead title="Lifecycle" sub="Expiry, revocation, soulbound, forever state tracking. Phase 3." />
      <div className="v-card"><span className="v-muted">Lifecycle cards + timeline land here.</span></div>
    </>
  );
}
