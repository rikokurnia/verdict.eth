import { PageHead } from '@/components/app/app-shell';

export default function Page() {
  return (
    <>
      <PageHead title="Agents" sub="Rebalancer / ENSIP-25 / ENSIP-26 discovery and agent-readable records. Phase 4." />
      <div className="v-card"><span className="v-muted">Agent card + resolution flow + query simulation land here.</span></div>
    </>
  );
}
